/**
 * A generic Task type
 * @name Task
 * @example
 * export interface Task {
 *   run(time: Time): void;
 *   error(time: Time, e: Error): void;
 *   dispose(): void;
 * }
 * @type
 */
export { Task } from '@most/types'
