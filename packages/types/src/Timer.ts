/**
 * A Timer interface 
 * @name Timer
 * @example
 * export interface Timer {
 *   now(): Time;
 *   setTimer(f: () => any, delayTime: Delay): Handle;
 *  clearTimer(timerHandle: Handle): void;
 * }
 * @type
 */
export { Timer } from '@most/types'
