/**
 * A Task scheduled for a particular time
 * @name ScheduledTask
 * @example
 * export interface ScheduledTask {
 *   task: Task;
 *   run(): void;
 *   error(err: Error): void;
 *   dispose(): void;
 * }
 * @type
 */
export { ScheduledTask } from '@most/types'
